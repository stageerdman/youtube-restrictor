<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleExecutable</key>
    <string>YTRestrictor</string>
    <key>CFBundleIdentifier</key>
    <string>com.stage-ria.ytrestrictor-app</string>
    <key>CFBundleName</key>
    <string>YouTube Restrictor</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSMinimumSystemVersion</key>
    <string>13.0</string>
    <!-- Menu-bar-only, no Dock icon — belt and suspenders alongside the
         NSApp.setActivationPolicy(.accessory) runtime call in
         YTRestrictorApp.swift, which is what actually applies when
         running as a bare `swift run` binary during dev (no bundle,
         no Info.plist to read this key from). -->
    <key>LSUIElement</key>
    <true/>
</dict>
</plist>
